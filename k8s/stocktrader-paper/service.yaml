apiVersion: v1
kind: Service
metadata:
  name: stocktrader-dashboard
  namespace: papertrader
  labels:
    app: stocktrader
    component: dashboard
spec:
  selector:
    app: stocktrader
    component: dashboard
  ports:
    - name: http
      port: 5001
      targetPort: 5001
  type: ClusterIP
